apiVersion: 1

contactPoints:
  - orgId: 1
    name: email-alerts
    receivers:
      - uid: email-default
        type: email
        disableResolveMessage: false
        settings:
          # Set ALERT_EMAIL in .env, see .env.example.
          addresses: $ALERT_EMAIL
          singleEmail: false
